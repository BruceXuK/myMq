package com.bruce.mq.shared.config;

import org.springframework.context.annotation.Configuration;

/**
 * 共享的日志配置类
 * 提供统一的日志级别配置建议
 */
@Configuration
public class LoggingConfig {
    
    // 此类作为一个标记，表示共享日志配置
    // 实际的日志配置将在各服务的application.yml中定义
    
}